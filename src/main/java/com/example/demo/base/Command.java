package com.example.demo.base;

/*
 * Deze interface beschrijft wat een command is. Het heeft slechts
 * één methode nodig, namelijk de execute methode.
 */
public interface Command {
    void execute();
}